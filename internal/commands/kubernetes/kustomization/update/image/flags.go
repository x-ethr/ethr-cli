package image

var (
	file     string // the relative file path
	image    string // the updated image name
	name     string // the image reference - includes name:tag
	tag      string
	registry string
	test     bool = false
)
